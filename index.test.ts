import { ZapierEmbed } from "./index";

describe("zapier-embed-typescript-sdk", () => {
    it("initialize client", async () => {
        const zapierembed = new ZapierEmbed({
        });
    });
});
